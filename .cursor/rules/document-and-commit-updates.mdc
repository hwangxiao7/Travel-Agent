---
description: 每次有实质性更新都必须同步更新文档并与代码一起提交
alwaysApply: true
---

# 更新即写文档，并一起提交

对本仓库做任何**实质性改动**(新功能、增强、重构、bug 修复、prompt / 模型优化、依赖或架构调整)时,必须在**同一个 commit** 里同时更新文档,再提交到仓库。不要只提交代码而把文档留到"以后补"。

## 每次更新要在文档里写清楚

1. **用了什么技术 / 方案**:涉及的库、模型、API、算法或设计模式。
2. **做了哪些改进**:改了什么、为什么改(动机),以及预期收益(性能 / 成本 / token / 准确性 / 可维护性等)。
3. **影响范围**:涉及的模块 / 文件、行为变化、兼容性或迁移注意事项。

## 文档写在哪

- 追加一条带日期的条目到 `docs/CHANGELOG.md`(没有就创建),最新的放最上面。
- 若改动涉及架构 / 技术选型,同时更新 `docs/` 下对应的文档(如 `docs/架构技术选型.md`、`docs/项目技术分析.md`)。

`docs/CHANGELOG.md` 条目格式:

```markdown
## 2026-07-15 — 简短标题
- 技术/方案:...
- 改进:...(动机 + 预期收益)
- 影响范围:<改动的文件 / 模块>
```

## 提交要求

- 文档与相关代码放在**同一个 commit**;commit message 说明"做了什么 / 为什么"。
- 只在用户明确要求时才 push。
- 不要把无关的、别人正在改的文件一并提交——只提交本次改动涉及的文件。

## 反面例子

```text
# 坏:只提交代码,文档没动
git add backend/app/services/llm.py && git commit -m "tweak prompt"

# 好:代码 + CHANGELOG(+ 相关架构文档)一起提交
git add backend/app/services/llm.py docs/CHANGELOG.md
git commit -m "把固定指令移到 system message,降低 token 并提升准确性"
```
